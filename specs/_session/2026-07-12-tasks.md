# Session task log — 2026-07-12

## Pending
- [ ] 21:32 — Create `bardstore` on PROD: first real (non-mock) store. Owner Mohamed Bardouni, phone +212708150351. BLOCKED: needs `store_type_id` (single FK — Tailor vs ready-to-wear undecided). Must go through real `POST /api/register-seller`, not raw SQL. Note: Bash sandbox blocks SSH → cannot reach Contabo directly.
- [ ] 21:32 — WhatsApp AI listing intake (greenfield — zero WhatsApp integration exists today). Flow: seller sends photo+caption → match `wa_id` → `users.phone` (phone-first auth) → AI drafts name/desc/category/price → bot asks quantity → seller confirms → create `Stock` + `ProductImage` → pending approval.
- [ ] 21:32 — Decide: auto-approve trusted sellers, else one-message listing still stalls in the approval queue.
- [ ] 21:45 — SCOPE: chat listing intake is for EXISTING sellers with an existing store only, matched on `users.phone`. No onboarding/store-creation over chat. Unknown sender → declined, never auto-registered.
- [ ] 21:45 — Add TELEGRAM as channel (phase 1 — free, no approval gate, ships first). WhatsApp = phase 2 (needs Meta approval). One shared intake pipeline + two thin channel adapters. Telegram does NOT expose phone → one-time `request_contact` handshake, persist `telegram_id` on users.
- [ ] 21:45 — **P0 PREREQUISITE BUG** — `users.phone` is inconsistently normalized: two competing helpers (`Support\Phone::normalize` vs `Support\PhoneNumber::normalizeMa`) AND `API/Mobile/AuthController.php:46` + `:311` write the phone RAW. DB holds a mix of `+212708150351` and `0708150351`. Chat channels deliver `212708150351` → naive lookup silently fails for every mobile-registered seller (the exact target users). Needs: consolidate normalizer, apply at all write sites, backfill migration (handle collisions), normalize-on-lookup.

- [ ] 22:10 — **PIVOT: PWA replaces Telegram as the listing surface.** Seller is already session-authenticated → no phone handshake, no webhook, no chat state machine, no Meta gate. Phone-normalization bug DEMOTED off critical path (still blocks a future WhatsApp adapter). Spec rewritten → `specs/014-quick-listing/`.
- [ ] 22:10 — Quick Listing design locked: multi-photo camera (no install required) + Darija voice note → AI drafts title/desc/category → COLOR auto-picked from image (override w/ 1 tap) → FABRIC always manual (never AI-guessed: silk vs chiffon unreliable from a photo → returns/trust damage) → price w/ AI-suggested range → qty/sizes as chips → one-screen review → publish. Install prompted only AFTER first successful listing (iOS needs it for Web Push; Android doesn't; Morocco is Android-heavy).
- [ ] 22:10 — **T0 RISK SPIKE: Darija speech-to-text quality is unproven.** Test on real recordings BEFORE building the voice path. Fallback = tap-to-select chips. Gates only the voice path; everything else proceeds.
- [ ] 22:10 — Rebuild `ColorExtractor` (in progress): current `ColorExtractionController` returns the BACKGROUND (samples whole frame → caftan on a white wall = "White"), buckets by exact hex so photo noise shatters one real color into thousands, and has English-only names. Fix = spatial background rejection + k-means quantization + AR/EN palette. Wire into listing draft.
- [ ] 22:10 — Build `stores:purge --keep=bardstore` (in progress): dry-run default, transactional, children-before-parents, refuses stores with real orders/revenue unless --force.
- [ ] 22:10 — STRATEGY: Beldify's competitor is Instagram/WhatsApp DMs, not Jumia. Cold-start solve = be a single-player TOOL first (seller brings her own buyers via a shareable link; Beldify handles checkout/COD/delivery/payout), then turn on marketplace discovery once supply+traffic accumulate. Narrow the wedge to women's traditional wear. Real moat = delivery + payout trust, not more AI features. North-star metric = orders completed & delivered per week.

- [ ] 22:40 — **ROOT CAUSE FOUND: dormant-vertical bug is NOT a null `store_type_id` — it's a WRONG one.** `beldify-frontend/src/app/seller/register/page.tsx:19-24` derives the store's VERTICAL from `business_type` (a LEGAL question): `{individual:1(Regular), company:2(Tailor), cooperative:3(Menswear)}`. So a company selling jewelry is filed as a TAILOR store → jewelry PDPs render the "sewn to measure" bespoke strip. And store_type_id 4 (Women's Clothing) + 5 (Jewelry) are UNREACHABLE via signup — bardstore literally cannot be created correctly through the form. Underlying cause: **no `GET /api/store-types` endpoint exists**, so the FE hardcoded a bogus map. FIX (in progress): add the endpoint + ask "what do you sell?" as its own question; keep `business_type` as the separate legal field it already is.
- [x] 22:40 — Checked & CLEARED: FE posts to `/api/auth/register-seller`; `route:list` confirms that URI is real (routes sit in a `prefix('auth')` group). NOT a bug — verified before reporting.

## Done
- [x] 22:50 — FRONTEND half of the vertical fix: deleted `BUSINESS_TYPE_TO_STORE_TYPE`; added `StoreTypePicker` + `useStoreTypes` hook (fed by `GET /api/store-types`), a real "What do you sell?" question on both `seller/register` + `seller/signup`, i18n keys across all 7 locales, and NO silent-default fallback (a failed fetch shows an error + retry rather than sending a guessed store_type_id). **41/41 tests pass, lint clean.** ✓ 22:50
- [x] 22:50 — `ColorExtractor` verified independently: 7 passed / 32 assertions. Caftan-on-white-wall now returns the caftan's color, not white; white-on-dark still returns white (background rejection is spatial, not "drop white"); AR+EN names on every result. ✓ 22:50
- [x] 22:50 — `stores:purge --keep=<id|slug|name>` built: dry-run default, transactional, children-before-parents, REFUSES stores with real orders/revenue/commissions/payouts unless `--force`. 12/12 tests. Key discovery: deleting a store CASCADES into orders/order_items/store_revenues/commissions/payout_requests/carts/custom_orders — a naive DELETE would have silently destroyed buyer order history. ✓ 22:50
