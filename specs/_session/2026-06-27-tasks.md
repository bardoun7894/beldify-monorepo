## Pending

- [ ] Fix multi-seller order splitting in OrderService::createCheckoutOrder()
- [ ] Check Blade views for frontend-registered customers
- [ ] Check Blade cart views
- [ ] Check all Blade sidebars for page consistency

## Done

- [x] Fixed 3 failing multi-seller tests (coupon v1 scope, checkout_group_id → group_id/group_number)
- [x] Verified multi-seller splitting is FULLY IMPLEMENTED in OrderService::createCheckoutOrder()
- [x] 32/32 multi-seller tests pass
- [x] Verified frontend T024-T027 already consume per-seller shapes (cart grouping, checkout quote, confirmation, history)
- [x] Updated tasks.md: 29/31 tasks complete (only T029 docs + T031 review remain)
- [x] Created OKF files (index.md + summary.md)
- [x] Checked Blade admin sidebar-v3 (clean, marketplace-focused), seller sidebar (minimal), admin carts (5 views), admin customers (table with translations)
- [x] Docker named-volume constraint documented
- [x] Memory updated

- [ ] Check customer data: do frontend registrations appear in Laravel admin customers table?
- [ ] Check cart data: do guest + user carts appear in admin carts Blade?
- [ ] Check order data: do frontend orders appear in admin orders Blade?
- [ ] Check wishlist data visibility
- [ ] Verify all Blade sidebar pages are connected to real data
- [ ] One-by-one page check: every Blade route → controller → view → data

## Done

- [x] Traced full customer registration: AuthController::register() → User + Customer + Role + Preferences
- [x] Verified Admin CustomerController::index() shows all frontend customers (company/branch scoping removed)
- [x] Verified Admin CartController::index() shows all carts (guest + user)
- [x] Fixed cart_table.blade.php: null-safe $cart->user for guest carts + route name admin.carts.show
- [x] Fixed abandoned.blade.php: removed dead carts.send-recovery route + fixed carts.show → admin.carts.show
- [x] Verified Admin OrderController::index() passes $orders with filters (real data, not empty)
- [x] Dead code found: OrderManagementController routes overwritten by Admin\OrderController prefix group
- [x] Verified all 18 admin controllers pass real data to views
- [x] Verified all sidebar-v3 route names resolve correctly
- [x] Cart model supports guest_token — guest carts stored and visible
- [x] Order model has customer relationship — orders visible with customer names in admin

- [ ] Fix dead code: remove OrderManagementController + duplicate routes
- [ ] Commit all changes + push
- [ ] Deploy to production server
